# Server Configuration
PORT=8000

# MongoDB Configuration
MONGODB_URI=your_mongodb_connection_string_here

# CORS Configuration (Optional - if you want to restrict origins)
# CORS_ORIGIN=http://localhost:3000
